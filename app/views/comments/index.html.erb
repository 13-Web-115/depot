<h1><%= t('.comment_list') %></h1>

<table>
  <tr>
    <th><%= t('.content') %></th>
    <th><%= t('.poster') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.content %></td>
    <td><%= comment.poster %></td>
    <td><%= link_to t('.show'), comment %></td>
    <td><%= link_to t('.edit'), edit_comment_path(comment) %></td>
    <td><%= link_to t('.destroy'), comment, method: :delete, data: { confirm: t('.sure_delete？') } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('.new_comment'), new_comment_path %>
