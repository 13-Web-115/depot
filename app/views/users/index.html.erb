<h1><%= t('.user_list') %></h1>
<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<table>
  <tr>
    <th><%= t('.name') %></th>
    <th><%= t('.type') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.genre %></td>
    <td><%= link_to t('.show'), user %></td>
    <td><%= link_to t('.edit'), edit_user_path(user) %></td>
    <td><%= link_to t('.destroy'), user, method: :delete, data: { confirm: t('.sure_delete？') } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('.new_user'), new_user_path %>
