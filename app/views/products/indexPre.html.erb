<h1><%= t('.product_list') %></h1>

<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.description') %></th>
    <th><%= t('.image url') %></th>
    <th><%= t('.price') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.title %></td>
    <td><%= product.description %></td>
    <td><%= product.image_url %></td>
    <td><%= product.price %></td>
    <td><%= link_to t('.show'), product %></td>
    <td><%= link_to t('.edit'), edit_product_path(product) %></td>
    <td><%= link_to t('.destroy'), product, method: :delete, data: { confirm: t('.sure_delete？') } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('.new_product'), new_product_path %>
